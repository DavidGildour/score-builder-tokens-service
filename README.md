# score-builder-tokens-service
Token dealer service for the Score Builder project.

Before running the app, make sure to edit `credentials_blank.py` with your own JWT key and rename the file to `credentials.py`.
